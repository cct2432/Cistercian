[project]
name = "cistercian"
version = "0.1.0"
description = "Converts 4-digit integers into Cistercian numeral SVG glyphs"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "cct2432" }]
dependencies = []
